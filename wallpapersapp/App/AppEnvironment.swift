import SwiftUI

struct AppEnvironment {
	static let appName = "Snapper"
}

extension Color {
	static var primary : Color {
		Color.black.opacity(0.9)
	}
	
	static var secondary : Color {
		Color.white.opacity(0.9)
	}
}
